#include <stdio.h>
#include <stdlib.h>

int main(int argc, const char *argv[]) {

    char name[30];

    gets(name); // Read string
    puts(name); // Write string

    system("PAUSE");
    return 0;
}
